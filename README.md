# xnetd

Attaches rootless podman containers to rootful netavark bridges (dual-stack) via podman's libnetwork. Apache-2.0, © 2026 James Braid. Go 1.24, static (`CGO_ENABLED=0`), zero build tags. Every `.go` file starts with `// SPDX-License-Identifier: Apache-2.0`.
