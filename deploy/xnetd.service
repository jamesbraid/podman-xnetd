[Unit]
Description=xnetd rootful-networking daemon
Requires=xnetd.socket
After=xnetd.socket
[Service]
Type=notify
ExecStart=/usr/local/bin/xnetd --config /etc/xnetd/config.toml
ExecReload=/bin/kill -HUP $MAINPID
WatchdogSec=30
Restart=on-failure
[Install]
WantedBy=multi-user.target
